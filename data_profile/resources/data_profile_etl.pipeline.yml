# The main pipeline for data_profile

resources:
  pipelines:
    data_profile_etl:
      name: data_profile_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/data_profile_etl"

      libraries:
        - glob:
            include: ../src/data_profile_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
